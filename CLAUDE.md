# Project instructions for Claude Code

Read `AGENTS.md` first — it is the operating protocol for BMAD × beads in this repo.

- Planning: BMAD skills (`/bmad-help` lists them). Outputs land in `_bmad-output/`.
- Execution state: beads (`bd`). `bd prime` runs on every session start via `.claude/settings.json`.
- Bridge: `uv run scripts/bmad_beads.py {import|sync|status|doctor}`.
- Standards and reference docs the team has adopted live in `docs/references/` (static copies, not plugins).

## Project facts

Stack, conventions, commands — fill in per project. Keep `AGENTS.md` generic; put specifics here.

- Stack:
- Run / test:
- Conventions:

<!-- `bd setup claude` appends its own "BEADS INTEGRATION" block below; keep it. -->
