## Bead

Closes/advances: `<bead-id>` (`bd show <bead-id>`)

## What changed

## Verification

- [ ] `task test` passes (if `scripts/` changed)
- [ ] `uv run scripts/bmad_beads.py sync` run; `status` shows the expected story state
- [ ] discovered work captured as beads, not left in prose
